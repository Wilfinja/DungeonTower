namespace DungeonTower.Core
{
    /// <summary>
    /// Stable identifier for a potion asset, set on the PotionSO in the
    /// Inspector. Starter entries are placeholders — add more as new
    /// potion assets get authored.
    /// </summary>
    public enum PotionId
    {
        HealthPotion,
        ManaPotion
    }
}
